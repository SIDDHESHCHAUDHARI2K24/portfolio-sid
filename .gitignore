# secrets & local config
.env
.env.*
!.env.example
opencode.json

# python
__pycache__/
*.py[cod]
.venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/

# node
node_modules/
.next/
dist/
.turbo/
*.tsbuildinfo

# playwright artifacts (baselines under tests/*-snapshots/ stay tracked)
playwright-report/
test-results/

# databases & dumps
*.db
*.sqlite
*.sql

# tooling indexes
.codegraph/
graphify-out/cost.json
graphify-out/cache/

# resume source PDFs — storage + DB are the source of truth (D8)
resumes/*.pdf
!resumes/.gitkeep

# os/editor
.DS_Store
.idea/
.vscode/

# local disk storage (dev runs with STORAGE_KIND=local)
backend/.storage/
